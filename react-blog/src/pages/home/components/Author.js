import React, { PureComponent } from 'react';
import { WriterWrapper } from '../style';

class Author extends PureComponent {

	render() {
		return (
			<WriterWrapper>Todo</WriterWrapper>
		)
	}
}

export default Author;
